---
ansible_connection: ansible.netcommon.network_cli
ansible_network_os: community.routeros.routeros

# see `sops-mikrotik.yaml`
ansible_user: "{{ mikrotik_cicd_username }}"
ansible_password: "{{ mikrotik_cicd_password }}"
...
